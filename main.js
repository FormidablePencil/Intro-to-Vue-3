

const app = Vue.createApp({
    data() { /* data is a predefined method */
        return {
            product: 'Socks',
            // Solution
            description: 'A warm fuzzy pair of socks.' 
            // Solution
        }
    }
})
